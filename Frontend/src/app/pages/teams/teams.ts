import { Component } from '@angular/core';
import { Navbar } from '../../layout/navbar/navbar';

@Component({
  selector: 'app-teams',
  imports: [Navbar],
  templateUrl: './teams.html',
  styleUrl: './teams.scss',
})
export class Teams {}
